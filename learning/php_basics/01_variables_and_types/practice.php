<?php
/**
 * ============================================================
 * PHP 基础学习 - 第1课：变量与数据类型
 * ============================================================
 * 
 * 📚 学习目标：
 *    1. 理解PHP变量的声明和命名规则
 *    2. 掌握PHP的8种基本数据类型
 *    3. 学会类型检测和类型转换
 *    4. 了解常量的定义和使用
 * 
 * 🔧 运行方式：
 *    php practice.php
 * 
 * ============================================================
 */

echo "=" . str_repeat("=", 60) . "\n";
echo "  第1课：变量与数据类型\n";
echo "=" . str_repeat("=", 60) . "\n\n";

// ============================================================
// 1. 变量基础
// ============================================================
echo "【1. 变量基础】\n";
echo str_repeat("-", 40) . "\n";

/**
 * 🔑 变量规则：
 * 1. 变量以 $ 符号开始
 * 2. 变量名必须以字母或下划线开头（不能以数字开头）
 * 3. 变量名只能包含字母、数字和下划线（A-z, 0-9, _）
 * 4. 变量名区分大小写（$name 和 $Name 是不同的变量）
 */

// ✅ 正确的变量命名
$name = "张三";            // 普通变量
$_private = "私有数据";    // 下划线开头（通常表示私有）
$userName = "李四";        // 驼峰命名（推荐）
$user_name = "王五";       // 下划线命名（也常见）
$name2 = "赵六";           // 数字在中间或末尾都可以

echo "姓名：$name\n";
echo "用户名（驼峰）：$userName\n";
echo "用户名（下划线）：$user_name\n\n";

/**
 * ⚠️ 常见错误 #1：变量名以数字开头
 * 
 * 错误示例：
 *   $2name = "test";  // ❌ 语法错误！
 * 
 * 正确示例：
 *   $name2 = "test";  // ✅ 正确
 */

/**
 * ⚠️ 常见错误 #2：变量名大小写混淆
 * 
 * $Name = "张三";
 * echo $name;  // ❌ 注意：$Name 和 $name 是不同的变量！
 * 
 * PHP 变量名是区分大小写的！
 */

// 演示大小写敏感
$Age = 25;
$age = 30;
echo "大小写敏感演示：\n";
echo "  \$Age = $Age\n";
echo "  \$age = $age\n";
echo "  （这是两个不同的变量！）\n\n";


// ============================================================
// 2. 数据类型
// ============================================================
echo "【2. PHP的8种数据类型】\n";
echo str_repeat("-", 40) . "\n";

/**
 * 📦 PHP 数据类型分类：
 * 
 * 标量类型（4种）：
 *   - string（字符串）
 *   - integer（整数）
 *   - float/double（浮点数）
 *   - boolean（布尔值）
 * 
 * 复合类型（2种）：
 *   - array（数组）
 *   - object（对象）
 * 
 * 特殊类型（2种）：
 *   - null（空值）
 *   - resource（资源）
 */

// 2.1 字符串 (String)
echo "\n2.1 字符串 (String)：\n";
$str1 = 'Hello';           // 单引号字符串
$str2 = "World";           // 双引号字符串
$str3 = "Hello, $name";    // 双引号支持变量解析
$str4 = 'Hello, $name';    // 单引号不解析变量

echo "  单引号：$str1\n";
echo "  双引号：$str2\n";
echo "  双引号变量解析：$str3\n";
echo "  单引号不解析：$str4\n";

/**
 * 💡 单引号 vs 双引号：
 * 
 * 单引号 ''：
 *   - 原样输出，不解析变量
 *   - 性能略好（因为不需要解析）
 *   - 只识别 \\ 和 \'
 * 
 * 双引号 ""：
 *   - 会解析变量和转义字符
 *   - 更灵活，适合拼接变量
 *   - 识别 \n, \t, \r, \\, \$, \" 等
 */

// 2.2 整数 (Integer)
echo "\n2.2 整数 (Integer)：\n";
$int1 = 100;        // 十进制
$int2 = -200;       // 负数
$int3 = 0755;       // 八进制（以0开头）
$int4 = 0xFF;       // 十六进制（以0x开头）
$int5 = 0b1111;     // 二进制（以0b开头，PHP 5.4+）

echo "  十进制：$int1\n";
echo "  负数：$int2\n";
echo "  八进制 0755 = " . octdec('755') . "（十进制）\n";
echo "  十六进制 0xFF = " . hexdec('FF') . "（十进制）\n";
echo "  二进制 0b1111 = " . bindec('1111') . "（十进制）\n";

// 2.3 浮点数 (Float/Double)
echo "\n2.3 浮点数 (Float)：\n";
$float1 = 3.14;
$float2 = 3.14e2;    // 科学计数法：3.14 × 10²
$float3 = 3.14e-2;   // 科学计数法：3.14 × 10⁻²

echo "  普通浮点数：$float1\n";
echo "  科学计数法 3.14e2：$float2\n";
echo "  科学计数法 3.14e-2：$float3\n";

/**
 * ⚠️ 常见错误 #3：浮点数比较
 * 
 * 由于浮点数精度问题，不要直接比较浮点数是否相等！
 * 
 * 错误示例：
 *   if (0.1 + 0.2 == 0.3) { ... }  // ❌ 可能返回false！
 * 
 * 正确示例：
 *   if (abs((0.1 + 0.2) - 0.3) < 0.00001) { ... }  // ✅ 使用精度范围
 */
echo "\n  ⚠️ 浮点数精度问题演示：\n";
$result = 0.1 + 0.2;
echo "  0.1 + 0.2 = $result\n";
echo "  0.1 + 0.2 == 0.3? " . (0.1 + 0.2 == 0.3 ? "true" : "false") . "\n";
echo "  （实际上 0.1+0.2 = 0.30000000000000004）\n";

// 2.4 布尔值 (Boolean)
echo "\n2.4 布尔值 (Boolean)：\n";
$isActive = true;
$isDeleted = false;

echo "  isActive：" . ($isActive ? "true" : "false") . "\n";
echo "  isDeleted：" . ($isDeleted ? "true" : "false") . "\n";

/**
 * 💡 以下值在布尔转换时为 false：
 *   - false
 *   - 0（整数零）
 *   - 0.0（浮点零）
 *   - ""（空字符串）
 *   - "0"（字符串零）
 *   - []（空数组）
 *   - null
 * 
 * 其他所有值都是 true
 */

echo "\n  布尔转换演示：\n";
$values = [0, 1, "", "hello", [], [1, 2], null, "0"];
foreach ($values as $val) {
    $type = gettype($val);
    $display = is_array($val) ? (empty($val) ? "[]" : "[...]") : var_export($val, true);
    $bool = $val ? "true" : "false";
    echo "    $display ($type) => $bool\n";
}

// 2.5 数组 (Array)
echo "\n2.5 数组 (Array)：\n";
$arr1 = [1, 2, 3];                          // 索引数组（简写）
$arr2 = array(1, 2, 3);                     // 索引数组（传统写法）
$arr3 = ['name' => '张三', 'age' => 25];    // 关联数组
$arr4 = [                                    // 混合数组
    'user' => ['name' => '李四', 'age' => 30],
    'scores' => [85, 90, 95]
];

echo "  索引数组：" . implode(", ", $arr1) . "\n";
echo "  关联数组：name = {$arr3['name']}, age = {$arr3['age']}\n";
echo "  嵌套数组：{$arr4['user']['name']} 的分数是 {$arr4['scores'][0]}\n";

// 2.6 对象 (Object)
echo "\n2.6 对象 (Object)：\n";

class Person
{
    /** @var string */
    public $name;

    /** @var int */
    public $age;

    public function __construct($name, $age)
    {
        $this->name = $name;
        $this->age = $age;
    }

    public function greet()
    {
        return "你好，我是{$this->name}，今年{$this->age}岁。";
    }
}

$person = new Person("张三", 25);
echo "  对象属性：{$person->name}, {$person->age}\n";
echo "  对象方法：{$person->greet()}\n";

// 2.7 NULL
echo "\n2.7 NULL（空值）：\n";
$nullVar = null;
$undefinedVar = null;  // 未定义的变量赋值为null

echo "  null变量：" . var_export($nullVar, true) . "\n";
echo "  is_null检测：" . (is_null($nullVar) ? "true" : "false") . "\n";

/**
 * 💡 NULL 的三种情况：
 * 1. 变量被赋值为 null
 * 2. 变量未被赋值
 * 3. 变量被 unset()
 */

// 2.8 Resource（资源）
echo "\n2.8 Resource（资源）：\n";
echo "  资源类型主要用于：文件句柄、数据库连接等\n";
echo "  例如：\$file = fopen('example.txt', 'r');\n";


// ============================================================
// 3. 类型检测
// ============================================================
echo "\n【3. 类型检测】\n";
echo str_repeat("-", 40) . "\n";

/**
 * 📋 常用类型检测函数：
 * 
 * gettype($var)      - 获取变量类型（返回字符串）
 * is_string($var)    - 是否是字符串
 * is_int($var)       - 是否是整数
 * is_float($var)     - 是否是浮点数
 * is_bool($var)      - 是否是布尔值
 * is_array($var)     - 是否是数组
 * is_object($var)    - 是否是对象
 * is_null($var)      - 是否是NULL
 * is_numeric($var)   - 是否是数字或数字字符串
 * isset($var)        - 变量是否存在且不为null
 * empty($var)        - 变量是否为空
 */

$testVars = [
    'string' => "Hello",
    'int' => 100,
    'float' => 3.14,
    'bool' => true,
    'array' => [1, 2, 3],
    'null' => null,
];

echo "类型检测示例：\n";
foreach ($testVars as $name => $var) {
    printf("  %-8s => %-10s\n", $name, gettype($var));
}

// isset vs empty 区别
echo "\nisset vs empty 区别：\n";
$testValue = "";
echo "  \$testValue = '' (空字符串)\n";
echo "  isset(\$testValue): " . (isset($testValue) ? "true" : "false") . "\n";
echo "  empty(\$testValue): " . (empty($testValue) ? "true" : "false") . "\n";

/**
 * ⚠️ 常见错误 #4：isset 和 empty 的混淆
 * 
 * isset() - 检查变量是否设置且不为null
 *   - $var = 0;      isset($var) => true
 *   - $var = "";     isset($var) => true
 *   - $var = null;   isset($var) => false
 *   - 未定义变量     isset($var) => false
 * 
 * empty() - 检查变量是否为空（包含多种情况）
 *   - $var = 0;      empty($var) => true
 *   - $var = "";     empty($var) => true
 *   - $var = null;   empty($var) => true
 *   - $var = [];     empty($var) => true
 *   - $var = false;  empty($var) => true
 */


// ============================================================
// 4. 类型转换
// ============================================================
echo "\n【4. 类型转换】\n";
echo str_repeat("-", 40) . "\n";

// 4.1 强制类型转换
echo "\n4.1 强制类型转换（使用类型标识符）：\n";
$num = 100;
$str = (string) $num;     // 转为字符串
$float = (float) $num;    // 转为浮点数
$bool = (bool) $num;      // 转为布尔值
$arr = (array) $num;      // 转为数组

echo "  原始值：$num (integer)\n";
echo "  (string)：'$str' (" . gettype($str) . ")\n";
echo "  (float)：$float (" . gettype($float) . ")\n";
echo "  (bool)：" . ($bool ? "true" : "false") . " (" . gettype($bool) . ")\n";
echo "  (array)：" . print_r($arr, true);

// 4.2 使用函数转换
echo "\n4.2 使用函数转换：\n";
$strNum = "123.45abc";
echo "  原始字符串：'$strNum'\n";
echo "  intval()：" . intval($strNum) . "\n";      // 123
echo "  floatval()：" . floatval($strNum) . "\n";  // 123.45
echo "  strval()：'" . strval(123) . "'\n";        // "123"

/**
 * ⚠️ 常见错误 #5：字符串转数字的陷阱
 * 
 * PHP 8.0 以后的变化：
 * - "123abc" + 0 在 PHP 8.0 会产生 TypeError
 * - 使用 intval() 或 (int) 更安全
 * 
 * 推荐做法：
 *   使用 is_numeric() 先检查
 *   使用 intval() / floatval() 进行转换
 */


// ============================================================
// 5. 常量
// ============================================================
echo "\n【5. 常量】\n";
echo str_repeat("-", 40) . "\n";

/**
 * 📌 常量特点：
 * 1. 一旦定义，不能修改或取消
 * 2. 常量名默认区分大小写
 * 3. 常量可以在任何地方访问（全局作用域）
 * 4. 常量只能存储标量值（PHP 7.0+ 可存储数组）
 */

// 5.1 使用 define() 定义
define('SITE_NAME', 'PHP学习网站');
define('MAX_LOGIN_ATTEMPTS', 5);
define('SUPPORTED_LANGUAGES', ['zh', 'en', 'ja']);  // PHP 7.0+

echo "define() 定义的常量：\n";
echo "  SITE_NAME：" . SITE_NAME . "\n";
echo "  MAX_LOGIN_ATTEMPTS：" . MAX_LOGIN_ATTEMPTS . "\n";
echo "  SUPPORTED_LANGUAGES：" . implode(", ", SUPPORTED_LANGUAGES) . "\n";

// 5.2 使用 const 定义（编译时定义）
const APP_VERSION = '1.0.0';
const DEBUG_MODE = true;

echo "\nconst 定义的常量：\n";
echo "  APP_VERSION：" . APP_VERSION . "\n";
echo "  DEBUG_MODE：" . (DEBUG_MODE ? "true" : "false") . "\n";

/**
 * 💡 define() vs const：
 * 
 * define()：
 *   - 运行时定义
 *   - 可以在条件语句中使用
 *   - 必须使用标量表达式
 * 
 * const：
 *   - 编译时定义
 *   - 不能在条件语句中使用
 *   - 语法更简洁
 *   - 在类中定义类常量必须用 const
 */

// 5.3 预定义的魔术常量
echo "\n预定义魔术常量：\n";
echo "  __FILE__：" . __FILE__ . "\n";     // 当前文件路径
echo "  __LINE__：" . __LINE__ . "\n";     // 当前行号
echo "  __DIR__：" . __DIR__ . "\n";       // 当前目录
echo "  __FUNCTION__：" . __FUNCTION__ . "\n";  // 当前函数名（在函数外为空）

function showMagicConstants()
{
    echo "  在函数内 __FUNCTION__：" . __FUNCTION__ . "\n";
}
showMagicConstants();


// ============================================================
// 6. 练习题
// ============================================================
echo "\n" . str_repeat("=", 60) . "\n";
echo "【练习题】\n";
echo str_repeat("=", 60) . "\n";

/**
 * 练习1：创建变量
 * 创建以下变量并输出：
 * - 你的名字
 * - 你的年龄
 * - 是否在学习PHP（布尔值）
 */
echo "\n练习1：创建变量（请取消注释并填写）\n";
// $myName = "";      // 填写你的名字
// $myAge = 0;        // 填写你的年龄
// $isLearning = ;    // 填写 true 或 false
// echo "我是$myName，今年$myAge岁，" . ($isLearning ? "正在" : "没有") . "学习PHP。\n";

/**
 * 练习2：类型转换
 * 将字符串 "42" 转换为整数，并与 8 相加
 */
echo "\n练习2：类型转换（请取消注释并填写）\n";
// $strNumber = "42";
// $result = ;  // 将 $strNumber 转换为整数并加8
// echo "$strNumber + 8 = $result\n";

/**
 * 练习3：数组操作
 * 创建一个包含3门课程成绩的关联数组，并计算平均分
 */
echo "\n练习3：数组操作（请取消注释并填写）\n";
// $scores = [
//     'math' => ,      // 填写数学成绩
//     'english' => ,   // 填写英语成绩
//     'php' => ,       // 填写PHP成绩
// ];
// $average = ;  // 计算平均分
// echo "平均分：$average\n";


echo "\n" . str_repeat("=", 60) . "\n";
echo "  第1课学习完成！\n";
echo "  下一课：02_operators（运算符）\n";
echo str_repeat("=", 60) . "\n";
