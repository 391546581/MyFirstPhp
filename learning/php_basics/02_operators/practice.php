<?php
/**
 * ============================================================
 * PHP 基础学习 - 第2课：运算符
 * ============================================================
 * 
 * 📚 学习目标：
 *    1. 掌握算术运算符
 *    2. 理解赋值运算符和复合赋值
 *    3. 掌握比较运算符（特别是 == 和 ===）
 *    4. 理解逻辑运算符
 *    5. 学会使用字符串运算符
 *    6. 了解运算符优先级
 * 
 * 🔧 运行方式：
 *    php practice.php
 * 
 * ============================================================
 */

echo "=" . str_repeat("=", 60) . "\n";
echo "  第2课：运算符\n";
echo "=" . str_repeat("=", 60) . "\n\n";

// ============================================================
// 1. 算术运算符
// ============================================================
echo "【1. 算术运算符】\n";
echo str_repeat("-", 40) . "\n";

$a = 10;
$b = 3;

echo "设 \$a = $a, \$b = $b\n\n";

/**
 * 📋 算术运算符列表：
 * +  加法
 * -  减法
 * *  乘法
 * /  除法
 * %  取模（取余数）
 * ** 幂运算（PHP 5.6+）
 */

printf("  加法：\$a + \$b = %d\n", $a + $b);    // 13
printf("  减法：\$a - \$b = %d\n", $a - $b);    // 7
printf("  乘法：\$a * \$b = %d\n", $a * $b);    // 30
printf("  除法：\$a / \$b = %.2f\n", $a / $b);  // 3.33
printf("  取模：\$a %% \$b = %d\n", $a % $b);   // 1
printf("  幂运算：\$a ** 2 = %d\n", $a ** 2);   // 100

/**
 * ⚠️ 常见错误 #1：除以零
 * 
 * 在 PHP 8.0+，除以零会产生 DivisionByZeroError
 * 
 * 错误示例：
 *   $result = 10 / 0;  // ❌ 抛出错误
 * 
 * 正确做法：
 *   if ($divisor != 0) {
 *       $result = $dividend / $divisor;
 *   }
 */

/**
 * ⚠️ 常见错误 #2：取模运算与负数
 * 
 * PHP 的取模运算结果符号与被除数相同：
 * 
 *   10 % 3  = 1
 *   -10 % 3 = -1  // 注意：不是 2
 *   10 % -3 = 1   // 不是 -2
 */
echo "\n取模与负数：\n";
echo "  -10 % 3 = " . (-10 % 3) . " （符号跟随被除数）\n";
echo "  10 % -3 = " . (10 % -3) . " （符号跟随被除数）\n";

// 自增和自减
echo "\n自增/自减运算符：\n";
$x = 5;
echo "  初始值 \$x = $x\n";
echo "  前置自增 ++\$x = " . (++$x) . "（先加后用，现在 \$x = $x）\n";

$x = 5;
echo "  \$x 重置为 $x\n";
echo "  后置自增 \$x++ = " . ($x++) . "（先用后加，现在 \$x = $x）\n";

/**
 * 💡 ++$x vs $x++：
 * 
 * 前置 ++$x：先自增，再返回值
 * 后置 $x++：先返回值，再自增
 * 
 * 这个区别在循环中特别重要！
 */


// ============================================================
// 2. 赋值运算符
// ============================================================
echo "\n【2. 赋值运算符】\n";
echo str_repeat("-", 40) . "\n";

/**
 * 📋 赋值运算符列表：
 * =    基本赋值
 * +=   加后赋值
 * -=   减后赋值
 * *=   乘后赋值
 * /=   除后赋值
 * %=   取模后赋值
 * **=  幂运算后赋值
 * .=   字符串连接后赋值
 * ??=  空合并赋值（PHP 7.4+）
 */

$num = 10;
echo "初始值 \$num = $num\n";

$num += 5;
echo "  \$num += 5  => $num\n";  // 15
$num -= 3;
echo "  \$num -= 3  => $num\n";  // 12
$num *= 2;
echo "  \$num *= 2  => $num\n";  // 24
$num /= 4;
echo "  \$num /= 4  => $num\n";  // 6
$num %= 4;
echo "  \$num %= 4  => $num\n";  // 2

// 字符串连接赋值
$str = "Hello";
$str .= " World";
echo "\n字符串连接：\$str .= \" World\" => $str\n";

// 空合并赋值（PHP 7.4+）
echo "\n空合并赋值（??=）：\n";
$value = null;
$value ??= "默认值";  // 如果 $value 为 null，则赋值
echo "  \$value = null; \$value ??= \"默认值\" => $value\n";

$value2 = "已有值";
$value2 ??= "默认值";  // $value2 不为 null，不赋值
echo "  \$value2 = \"已有值\"; \$value2 ??= \"默认值\" => $value2\n";


// ============================================================
// 3. 比较运算符（重点！）
// ============================================================
echo "\n【3. 比较运算符】⭐ 重要\n";
echo str_repeat("-", 40) . "\n";

/**
 * 📋 比较运算符列表：
 * ==   等于（值相等）
 * ===  全等（值和类型都相等）
 * !=   不等于
 * <>   不等于（同 !=）
 * !==  不全等
 * <    小于
 * >    大于
 * <=   小于等于
 * >=   大于等于
 * <=>  太空船运算符（PHP 7+）
 */

echo "== vs === （重要区别）：\n\n";

/**
 * ⚠️ 常见错误 #3：== 和 === 的混淆（最常见的PHP新手错误之一！）
 * 
 * ==  松散比较：只比较值，会进行类型转换
 * === 严格比较：比较值和类型，不进行类型转换
 * 
 * 强烈建议：大多数情况下使用 ===
 */

// 松散比较的陷阱
$testCases = [
    ['0', 0],
    ['', 0],
    [null, false],
    ['php', 0],    // PHP 7 中会是 true（字符串转为0）
    [[], false],
    ['1', 1],
    [true, 1],
    [true, 'hello'],
];

echo "松散比较 (==) vs 严格比较 (===)：\n";
echo str_repeat("-", 50) . "\n";
printf("  %-15s %-10s %-10s\n", "比较", "==", "===");
echo str_repeat("-", 50) . "\n";

foreach ($testCases as [$left, $right]) {
    $leftStr = var_export($left, true);
    $rightStr = var_export($right, true);
    $loose = $left == $right ? 'true' : 'false';
    $strict = $left === $right ? 'true' : 'false';
    printf("  %-15s %-10s %-10s\n", "$leftStr vs $rightStr", $loose, $strict);
}

echo "\n💡 最佳实践：优先使用 === 进行比较！\n";

// 太空船运算符（PHP 7+）
echo "\n太空船运算符 <=>：\n";
echo "  返回值：-1（小于）、0（等于）、1（大于）\n";
echo "  1 <=> 2 = " . (1 <=> 2) . "\n";  // -1
echo "  2 <=> 2 = " . (2 <=> 2) . "\n";  // 0
echo "  3 <=> 2 = " . (3 <=> 2) . "\n";  // 1

// 太空船运算符在排序中的应用
echo "\n  应用：数组排序\n";
$numbers = [3, 1, 4, 1, 5, 9, 2, 6];
usort($numbers, fn($a, $b) => $a <=> $b);  // 升序
echo "  升序排序：" . implode(", ", $numbers) . "\n";

usort($numbers, fn($a, $b) => $b <=> $a);  // 降序
echo "  降序排序：" . implode(", ", $numbers) . "\n";


// ============================================================
// 4. 逻辑运算符
// ============================================================
echo "\n【4. 逻辑运算符】\n";
echo str_repeat("-", 40) . "\n";

/**
 * 📋 逻辑运算符列表：
 * &&  或 and   逻辑与
 * ||  或 or    逻辑或
 * !           逻辑非
 * xor         逻辑异或
 */

$t = true;
$f = false;

echo "设 \$t = true, \$f = false\n\n";
echo "  \$t && \$f  = " . ($t && $f ? "true" : "false") . "\n";   // false
echo "  \$t || \$f  = " . ($t || $f ? "true" : "false") . "\n";   // true
echo "  !\$t       = " . (!$t ? "true" : "false") . "\n";         // false
echo "  \$t xor \$f = " . ($t xor $f ? "true" : "false") . "\n";  // true

/**
 * ⚠️ 常见错误 #4：and/or 与 &&/|| 的优先级不同
 * 
 * && 和 || 的优先级高于 and 和 or
 * 
 * 陷阱示例：
 *   $result = true and false;  // $result = true！(先赋值，再and)
 *   $result = true && false;   // $result = false（先&&，再赋值）
 * 
 * 建议：统一使用 && 和 ||，避免 and 和 or
 */

echo "\n优先级陷阱演示：\n";
$result1 = true and false;  // = 先于 and 执行
$result2 = true && false;   // && 先于 = 执行
echo "  \$result1 = true and false => " . ($result1 ? "true" : "false") . "（陷阱！）\n";
echo "  \$result2 = true && false  => " . ($result2 ? "true" : "false") . "（正确）\n";

// 短路求值
echo "\n短路求值（Short-circuit evaluation）：\n";
/**
 * 💡 短路求值：
 * 
 * &&：如果左边为 false，右边不执行
 * ||：如果左边为 true，右边不执行
 * 
 * 这个特性常用于：
 * 1. 避免错误（如检查变量是否存在后再使用）
 * 2. 提供默认值
 */

$user = null;
// 利用短路求值安全访问
$name = $user && $user['name'] ? $user['name'] : '游客';
echo "  \$user = null 时获取名字：$name\n";


// ============================================================
// 5. 字符串运算符
// ============================================================
echo "\n【5. 字符串运算符】\n";
echo str_repeat("-", 40) . "\n";

/**
 * 📋 字符串运算符：
 * .   连接运算符
 * .=  连接赋值运算符
 */

$firstName = "张";
$lastName = "三";
$fullName = $firstName . $lastName;

echo "  \$firstName = \"$firstName\"\n";
echo "  \$lastName = \"$lastName\"\n";
echo "  \$firstName . \$lastName = \"$fullName\"\n";

// 多种字符串拼接方式比较
echo "\n字符串拼接的多种方式：\n";
$name = "PHP";
$version = "8.0";

// 方式1：点运算符
$str1 = "学习" . $name . " " . $version;
echo "  点运算符：$str1\n";

// 方式2：双引号内嵌变量
$str2 = "学习{$name} {$version}";
echo "  双引号变量：$str2\n";

// 方式3：sprintf格式化
$str3 = sprintf("学习%s %s", $name, $version);
echo "  sprintf：$str3\n";

// 方式4：数组implode
$str4 = implode(" ", ["学习", $name, $version]);
echo "  implode：$str4\n";


// ============================================================
// 6. 空合并运算符 ??（PHP 7+）
// ============================================================
echo "\n【6. 空合并运算符 ??】\n";
echo str_repeat("-", 40) . "\n";

/**
 * 💡 ?? 运算符：
 * 如果左边的值存在且不为 null，返回左边的值
 * 否则返回右边的值
 * 
 * 等同于：isset($var) ? $var : '默认值'
 */

$username = null;
$displayName = $username ?? '游客';
echo "  \$username = null\n";
echo "  \$displayName = \$username ?? '游客' => $displayName\n";

$username = '张三';
$displayName = $username ?? '游客';
echo "\n  \$username = '张三'\n";
echo "  \$displayName = \$username ?? '游客' => $displayName\n";

// 链式使用
$first = null;
$second = null;
$third = '默认值';
$result = $first ?? $second ?? $third;
echo "\n链式使用：\$first ?? \$second ?? \$third => $result\n";

/**
 * ⚠️ 常见错误 #5：?? 与 ?: 的区别
 * 
 * ?? 空合并运算符：检查是否为 null
 * ?: 三元运算符简写：检查是否为 falsy
 * 
 * $a = 0;
 * $a ?? 'default'    => 0（0 不是 null）
 * $a ?: 'default'    => 'default'（0 是 falsy）
 * 
 * $a = '';
 * $a ?? 'default'    => ''（空字符串不是 null）
 * $a ?: 'default'    => 'default'（空字符串是 falsy）
 */

echo "\n?? vs ?: 区别：\n";
$val = 0;
echo "  \$val = 0\n";
echo "  \$val ?? 'default' = " . ($val ?? 'default') . "\n";
echo "  \$val ?: 'default' = " . ($val ?: 'default') . "\n";

$val = '';
echo "\n  \$val = ''\n";
echo "  \$val ?? 'default' = '" . ($val ?? 'default') . "'\n";
echo "  \$val ?: 'default' = " . ($val ?: 'default') . "\n";


// ============================================================
// 7. 运算符优先级
// ============================================================
echo "\n【7. 运算符优先级】\n";
echo str_repeat("-", 40) . "\n";

/**
 * 📋 运算符优先级（从高到低，部分）：
 * 
 * **                  幂运算
 * ++ -- ~             递增/递减
 * !                   逻辑非
 * * / %               乘除取模
 * + - .               加减、字符串连接
 * << >>               位移
 * < <= > >=           比较
 * == != === !== <=>   相等比较
 * &&                  逻辑与
 * ||                  逻辑或
 * ??                  空合并
 * ?:                  三元运算
 * = += -= *= /= .=    赋值
 * and                 逻辑与（低优先级）
 * xor                 逻辑异或
 * or                  逻辑或（低优先级）
 */

echo "优先级示例：\n";

$result = 2 + 3 * 4;        // 3*4 先执行
echo "  2 + 3 * 4 = $result（先乘后加）\n";

$result = (2 + 3) * 4;      // 括号先执行
echo "  (2 + 3) * 4 = $result（括号优先）\n";

$result = 2 ** 3 ** 2;      // 幂运算右结合
echo "  2 ** 3 ** 2 = $result（幂运算右结合，即 2^(3^2) = 2^9 = 512）\n";

/**
 * 💡 最佳实践：
 * 
 * 1. 不确定优先级时，使用括号明确
 * 2. 复杂表达式拆分成多行
 * 3. 避免在一行中使用太多运算符
 */


// ============================================================
// 8. 练习题
// ============================================================
echo "\n" . str_repeat("=", 60) . "\n";
echo "【练习题】\n";
echo str_repeat("=", 60) . "\n";

/**
 * 练习1：计算表达式
 * 不运行代码，预测以下表达式的结果，然后验证
 */
echo "\n练习1：预测结果（请先思考，再取消注释验证）\n";
// $a = 5;
// $b = 2;
// echo "  5 + 3 * 2 = " . (5 + 3 * 2) . "\n";    // 预测：?
// echo "  10 / 4 = " . (10 / 4) . "\n";           // 预测：?
// echo "  10 % 3 = " . (10 % 3) . "\n";           // 预测：?
// echo "  2 ** 10 = " . (2 ** 10) . "\n";         // 预测：?

/**
 * 练习2：比较运算符
 * 预测以下比较的结果
 */
echo "\n练习2：比较运算符（请先思考，再取消注释验证）\n";
// echo "  '10' == 10：" . ('10' == 10 ? "true" : "false") . "\n";    // 预测：?
// echo "  '10' === 10：" . ('10' === 10 ? "true" : "false") . "\n";  // 预测：?
// echo "  0 == false：" . (0 == false ? "true" : "false") . "\n";    // 预测：?
// echo "  0 === false：" . (0 === false ? "true" : "false") . "\n";  // 预测：?

/**
 * 练习3：空合并运算符
 * 使用 ?? 运算符获取用户名，如果不存在则使用 "匿名用户"
 */
echo "\n练习3：空合并运算符（请取消注释并完成）\n";
// $userData = ['email' => 'test@example.com'];  // 注意：没有 name 字段
// $username = ;  // 使用 ?? 获取名字或默认值
// echo "  用户名：$username\n";

/**
 * 练习4：自增运算符
 * 理解前置和后置自增的区别
 */
echo "\n练习4：自增运算符（请先思考，再取消注释验证）\n";
// $i = 5;
// $j = 5;
// echo "  ++\$i的值：" . (++$i) . "，之后\$i = $i\n";
// echo "  \$j++的值：" . ($j++) . "，之后\$j = $j\n";


echo "\n" . str_repeat("=", 60) . "\n";
echo "  第2课学习完成！\n";
echo "  下一课：03_control_flow（流程控制）\n";
echo str_repeat("=", 60) . "\n";
